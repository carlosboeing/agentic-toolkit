# Roadmap

Forward view of the <PROJECT_NAME> project: what's in flight, queued, parked, and recently shipped. Single source of truth for "what's next" — no separate TODO file.

## In flight

Active work right now. Each item links to its design and (where applicable) plan.

(replace this with concrete in-flight items, or remove the placeholder)

## Next actions

Concrete things ready to do — just not started yet. When started, an item moves to **In flight**. Substantial Next actions get a `0-brainstorms/<topic>.md` with the full proposal; ROADMAP holds the one-line pointer.

(replace this with concrete next actions, or remove the placeholder)

## Drafts (pending review)

Designs / plans currently `status: draft` needing review before they unlock execution.

(replace this with a table of drafts, or remove the placeholder)

## Future considerations

On the radar but not designed yet — not actionable until designed. Trigger-based ("when X happens") is preferred over date-based for personal projects.

(replace with concrete items, or remove the placeholder)

## Open questions

True unknowns — things to find out or decide *before* they could become actions.

(replace with concrete questions, or remove the placeholder)

## Parked

Items considered and deferred — kept visible so they're not forgotten. Prefix each with PEP-1-derived vocabulary:

- **Deferred:** stalled or paused, may revisit (note the trigger that would unpark it)
- **Declined:** decided against after consideration (note the reasoning briefly)
- **Superseded:** replaced by another approach (link to the replacement)

Each entry should link to the parked brainstorm or design file (`0-brainstorms/<topic>.md` with `status: parked`).

(populated as items get parked)

## Recently shipped

Most recent first. Older entries live only in `CHANGELOG.md` once this section exceeds 10 entries or 90 days.

(populated as work ships)
